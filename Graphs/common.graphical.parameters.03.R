# Work in progress
# Functions to be explored.

box("plot", lty="dashed", lwd=2, col="red")
box("figure", lty="dotted", lwd=2, col="green")
box("inner", lty="dotdash", lwd=2, col="blue")
box("outer", lty="longdash", lwd=2, col="orange")

